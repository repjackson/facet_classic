.ui.inverted.stackable.big.menu
    .ui.container
        a.header.icon.item(href='/' class="{{isActivePath '/'}}") 
            h2 facet
        if currentUser
            .item #{currentUser.username}
            a#logout.ui.icon.item(href='#') 
                i.sign.out.icon
        else
            a.ui.icon.item(href='/sign-in') 
                i.sign.in.icon
