template(name='docs')
    .ui.hidden.divider
    each docs
        +doc

template(name='doc')
    .ui.basic.inverted.segment
        |!{body}

        // .extra
        //     div by {{author.username}} #{when}
        .meta
            each tags
                .ui.inverted.grey.button.tag(class=tag_class) #{this}
        // a.ui.icon.button(href="/view/#{_id}")
        //     i.angle.right.icon
        if is_author
            .edit.ui.icon.button
                i.pencil.icon
            // .clone.ui.icon.button
            //     i.fork.icon
            .delete.ui.icon.button
                i.remove.icon