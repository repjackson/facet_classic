template(name='docs')
    .ui.text.container
        +cloud
        each docs
            +doc

template(name='doc')
    .ui.fluid.segment
        |#{body}

        // .extra
        //     div by {{author.username}} #{when}
        .meta
            each tags
                .ui.small.button.tag(class=tag_class) #{this}
        // a.ui.icon.button(href="/view/#{_id}")
        //     i.angle.right.icon
        if is_author
            div
                .edit.ui.icon.button
                    i.pencil.icon
                .clone.ui.icon.button
                    i.fork.icon
                .delete.ui.icon.button
                    i.remove.icon